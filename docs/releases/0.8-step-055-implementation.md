# RFC-100 Step 055 — Implementation note (Option A)

**Status:** implemented on `feat/step-055-legacy-doc-type-canonical-flag` (pending review)  
**Architecture:** `docs/releases/0.8-step-055-architecture-review.md`  
**Baseline:** `2ae8561954a2b5be0e42810725fd4b024733beaa`

## Delivered

- Settings column **`legacy_doc_type_canonical_enabled`** (default **`false`**) via Alembic **`0019_legacy_doc_type_canonical_enabled`**
- ORM + Settings GET/PUT + `_RETRIEVAL_SETTING_FIELDS` invalidation awareness
- Helper `feature_flags.legacy_doc_type_canonical_enabled(settings)`
- RPS `finalize_pipeline` precedence gate + diagnostic `legacy_doc_type_canonical_path`
- Retrieval cache namespace includes the flag
- Migration flags panel + EN/UK labels/help

## Semantics

| Value | Behavior |
|-------|----------|
| `true` | Legacy `CanonicalSourceService.select_context` runs (if `enable_canonical_source_selection`) |
| `false` (default) | Skip doc-type reorder; keep post-DFP/broad-inject order |

## Explicitly unchanged

- Memory Assist (advisory)
- Memory canonical shadow (diagnostic)
- No Memory authority selector
- DocumentScorer / EA / Qdrant / corpus / Knowledge Profiles

## Rollback

`PUT /api/settings` with `legacy_doc_type_canonical_enabled: true`.

## Deploy note

Do **not** apply migration 0019 to `ai_site_agent` until an approved deploy. Release 0.8 is **not** complete (056–057 remain).

## Behavior comparison (unit scenario)

Overview query candidate pool: news (0.9) → about (0.4) → homepage (0.35), generic_corporate profile.

| Mode | Path diagnostic | Final order (source_id, type) | Classification |
|------|-----------------|-------------------------------|----------------|
| **A** `legacy_doc_type_canonical_enabled=true` | `enabled` | about → homepage (news demoted) | Parity with pre-055 |
| **B** default `false` | `skipped_flag_off` | news → about → homepage | **Expected** overview/news mix change; **quality risk** if live traffic relies on doc-type demotion |

Cache namespace `retrieval_settings_version` **differs** between true/false (**expected**).

Golden chat parity unit suite: **passed** under default false — **harmless** for current golden fixtures (no serious golden regression observed). Rollback remains Settings `true` if ops need pre-055 reorder.
