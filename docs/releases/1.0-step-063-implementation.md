# RFC-100 Step 063 — Default knowledge_os flags ON

**Status:** Engineering complete (pending Feature Acceptance / deploy)  
**Release:** 1.0 (in progress)  
**Product Readiness Gate:** **N/A** (backend defaults only; no Dashboard product surface)

## Scope (RFC-100)

Default Knowledge OS migration flags **ON** for Release 1.0 so the Executive → Reasoning → Evidence → Speech path (and related Memory/cache/diagnostics flags) is the default runtime posture.

## Explicitly NOT flipped ON

| Flag | Remains default | Why |
|------|-----------------|-----|
| `allow_legacy_kp_presets` | **false** | Legacy surface; RFC-101 forbids product presets happy path |
| `legacy_doc_type_canonical_enabled` | **false** | Legacy reorder path; keep disabled |

## Flags default ON (this step)

### Environment (config defaults)

- `KNOWLEDGE_OS_EXECUTIVE_ENABLED`
- `REASONING_SERVICE_ENABLED`
- `EVIDENCE_ASSEMBLY_ENABLED`
- `REASONING_SPEECH_ACTS_ENABLED`
- `MAINTENANCE_EXECUTION_ENABLED` (unset → enabled; still fail-closed on invalid values; budget still defaults to 0 → no work until budget set)

### Settings columns (model + Alembic `0020` + existing row UPDATE)

- `enable_semantic_diagnostics_v2`
- `cache_namespace_v2_enabled`
- `memory_shadow_write_enabled`
- `memory_evidence_assist_enabled`
- `memory_canonical_shadow_enabled`

## RFC-101 / RFC-102 mapping

| Concern | Mapping |
|---------|---------|
| RFC-101 | Ask “just works”; no product Settings flag grid (Build & flags = Engineering Mode only) |
| RFC-102 | No Dashboard module change required |
| Owner screen | N/A (backend) |

## Rollback

Set env flags to `false` and/or Settings columns to `false`; restart as needed. Migration downgrade restores server_default false (does not force-update rows back).

## Docs

- `docs/FEATURE_FLAGS.md` updated for 1.0 defaults  
- This file

## Product Readiness Gate

```yaml
Product Readiness Gate
  change: RFC-100 Step 063 — default knowledge_os flags ON
  surfaces_touched: none (backend env/settings defaults + Alembic 0020 + build_info)
  rfc_101_screens: []
  rfc_102_modules: []
  engineering_mode_only: false
  product_debt: none
  decision: N/A
  justification: backend flag default only; no Dashboard diff
```

## Dual Definition of Done

| Check | Result |
|-------|--------|
| RFC-100 Step 063 scope | Done (KOS defaults ON; legacy stay OFF) |
| Tests | See pytest / release-check |
| Docs | FEATURE_FLAGS + this file |
| RFC-101 compliant | Yes (no product Settings flag grid; Ask path remains product language) |
| RFC-102 compliant | N/A module change; no page monolith / nav change |
| Product Readiness Gate | **N/A** |
| No duplicated functionality/nav/ownership | Yes |
| Architectural violations | None |
