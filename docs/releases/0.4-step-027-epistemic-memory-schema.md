# Release 0.4 — Step 027: Epistemic Memory schema

**RFC-100 Step 027**  
**Lifecycle state:** Implementation → Engineering Ready (after review)  
**Migration:** `0014_epistemic_memory_tables`

## Summary

Additive PostgreSQL tables for Epistemic Memory substrate:

- `observation_ref`
- `claim`
- `evidence_link`

**Schema only.** No runtime wiring.

## Delivered

| Item | Path |
|------|------|
| Migration | `backend/migrations/versions/0014_epistemic_memory_tables.py` |
| ORM models | `backend/app/models/epistemic_memory.py` |
| Tests | `backend/tests/test_epistemic_memory_schema.py` |
| Docs | `docs/EPISTEMIC_MEMORY_SCHEMA.md` |

## Explicit non-goals (this step)

- EpistemicMemoryService (Step 028)
- Claim extraction / shadow writes (Steps 029–030)
- `memory_version` auto-bump (Step 031)
- Chat / retrieval / Executive / cache / indexing changes
- Dashboard UI
- New feature flags

## Validation

```bash
make release-check
cd backend && .venv/bin/pytest tests/test_epistemic_memory_schema.py -m unit -q
cd backend && .venv/bin/alembic upgrade head
```

## Rollback

```bash
cd backend && .venv/bin/alembic downgrade 0013_cache_namespace_v2_enabled
```

Drops the three tables only. No production behavior to restore.

## Next step

**Step 028** — `EpistemicMemoryService` read API (await review of 027).
