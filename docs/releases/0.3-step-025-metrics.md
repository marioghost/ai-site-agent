# RFC-100 Step 025 — Operational metrics

**Release track:** 0.3  
**Type:** Read-only observability — no behavior change.

## Endpoints

| Path | Response |
|------|----------|
| `GET /api/metrics` | Prometheus text/plain gauges |
| `GET /api/metrics/operational` | JSON snapshot |

## Gauges

| Name | Source |
|------|--------|
| `kos_memory_version` | `MemoryVersionService.get()` |
| `kos_knowledge_version` | `KnowledgeVersionService.get()` |

## Verify

```bash
curl -s http://localhost:8000/api/metrics
curl -s http://localhost:8000/api/metrics/operational
```

```bash
cd backend
.venv/bin/pytest tests/test_operational_metrics.py -m unit -q
```

See [MEMORY_VERSION.md](../MEMORY_VERSION.md#operational-metrics-step-025).
