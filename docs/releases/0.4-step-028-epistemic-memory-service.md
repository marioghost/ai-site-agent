# Release 0.4 — Step 028: EpistemicMemoryService read API

**RFC-100 Step 028**  
**Lifecycle state:** Implementation → Engineering Ready (after review)

## Summary

Internal read-only service for Epistemic Memory tables. **Not wired** into chat, retrieval, indexing, cache, or Executive.

## Delivered

| Item | Path |
|------|------|
| Service | `backend/app/services/epistemic_memory/epistemic_memory_service.py` |
| DTOs | `backend/app/services/epistemic_memory/types.py` |
| Tests | `backend/tests/test_epistemic_memory_service.py` |
| Docs | `docs/EPISTEMIC_MEMORY_SCHEMA.md` |

## Read methods

- `get_observation_ref` / `list_observation_refs`
- `get_claim` / `list_claims` / `list_claims_by_source_id`
- `list_evidence_links_for_claim`
- `get_summary`

## Explicit non-goals (this step)

- Shadow writes (Step 030)
- Claim extraction (Step 029)
- Auto `memory_version` bump (Step 031)
- HTTP API / dashboard
- Chat / retrieval / Executive / cache behavior changes

## Validation

```bash
make release-check
cd backend && .venv/bin/pytest tests/test_epistemic_memory_service.py -m unit -v
```

## Next step

**Step 029** — `ClaimExtractionFromSI` (await review of 028).
