# RFC-101 Product Completion — Product Debt Register

**Package:** G9-P1 (S001 seed), updated through S008
**Debt classes:** `none` · `accepted` · `must_resolve_before_1_0_acceptance` · `excluded`

| ID | Step | Description | Class | Owner | Status |
|----|------|-------------|-------|-------|--------|
| DEBT-S001-01 | S001 | Models / Answers / Access routes were scaffolds until S004 content migration | accepted | Dashboard Settings | resolved (S004) |
| DEBT-S001-02 | S001 | Legacy module URLs (`/overview`, `/chat`, `/users`, `/analytics`, `/logs`, `/sources`, `/indexing`, `/knowledge-profile`) remain bookmarkable as permanent redirect-compatibility shims | accepted | Dashboard IA | open (by design — permanent, not a gap) |
| EXCL-G11 | G11-P0 | Backend deferred (dual-read cache fallback, `kos_tension_resolved_total`, pipeline deferred adapters) **excluded** from Dashboard Product Completion packages | excluded | Backend (separate auth) | excluded |
| DEBT-S003-01 | S003 | Legacy `components/analytics/*` retained on disk for Overview preview until `G6-P2` | accepted | Dashboard Insights / G8-P4 | superseded — see DEBT-S008-01 (Overview preview itself removed in S007; the legacy tree is now fully orphaned, not merely "retained for preview") |
| DEBT-S003-02 | S003 | Ask `ChatHistoryModal` coexistence until `G3-P3` / S006 | accepted | Dashboard Ask | resolved (S006) |
| DEBT-S004-01 | S004 | `SettingsAdvancedSection`/`RetrievalEnginePanel`/`LlmRuntimePanel`/`MigrationFlagsPanel` remain in `components/settings` on disk pending `G7-P5` (S006) Eng Advanced/Build relocation | accepted | Dashboard Settings / Engineering | resolved for `LlmRuntimePanel` (still live/shared, not debt); superseded for the other three — see DEBT-S008-02 |
| DEBT-S004-02 | S004 | `pages/SettingsPage.tsx` left unrouted and unmodified (dead code, no live links); not converted to a redirect to avoid reopening frozen Step 065/S001 contract tests | accepted | Dashboard Settings / G8-P4 | open — confirmed still true at S008; see DEBT-S008-02 |
| DEBT-S005-01 | S005 | `ChatHistoryModal` and `ChatDiagnosticsSidebar` remain mounted in Ask pending `G3-P2`…`P4` (S006) history/diagnostics ownership move to Activity/Eng ask-details | accepted | Dashboard Ask | resolved (S006) |
| DEBT-S005-02 | S005 | Overview widgets remain in place pending `G6-P2` (S006/S007) redistribution to Insights/Eng status | accepted | Dashboard Home | resolved (S007) |
| DEBT-S005-03 | S005 | `/` default landing remains `/overview` pending `G6-P3` (S007) Home-as-default cutover | accepted | Dashboard IA | resolved (S007) |
| DEBT-S006-01 | S006 | Home-as-default and Overview widget redistribution remain deferred to S007 (`G6-P2`, `G6-P3`) | accepted | Dashboard Home | resolved (S007) |
| DEBT-S006-02 | S006 | `EpistemicHealthPage` remains a full standalone page (not converted to a redirect) — `EngTensionsScreen` links to it instead, per the package's explicit either/or authority | accepted | Dashboard Engineering | open — accepted permanent design choice, not a gap to close |
| DEBT-S007-01 | S007 | `AnalyticsPreviewRow.tsx` and `KnowledgeBaseStatusCard.tsx` fully unmounted (no importer) but not deleted | accepted | Dashboard architecture / G8-P4 | superseded — folded into the larger DEBT-S008-03 tree (both files are part of the same orphaned `components/overview/*` set) |
| DEBT-S007-02 | S007 | Unused `nav.overview` i18n key (en/uk) left in place — harmless, not wired into any nav | accepted | Dashboard i18n | open — confirmed still true at S008, no acceptance criterion requires removal |
| DEBT-S008-01 | S008 | `src/components/analytics/*` (12 files: `AnalyticsHeader`, `AnalyticsKpiCard`, `AnalyticsKpiSection`, `AnalyticsTrendsSection`, `DistributionBarChart`, `InsightsSections`, `PopularQueriesSection`, `ProblematicQueriesSection`, `RetrievalQualitySection`, `SourceAnalyticsSection`, `TrendChartCard`) fully superseded by `features/insights/performance/widgets/*` (S003 copy); zero live importers | accepted | Dashboard architecture / G8-P4 | open — not deleted; frozen-test protected (`s005HomeAskCutover.test.ts` reads `ProblematicQueriesSection.tsx?raw`) |
| DEBT-S008-02 | S008 | `pages/SettingsPage.tsx` (unrouted) + `components/settings/{SettingsAdvancedSection,RetrievalEnginePanel,MigrationFlagsPanel,SettingsHelpAccordion}.tsx` fully superseded by `features/settings/*` (S004) + `features/engineering/{advanced,build}/widgets/*` (S006 copy); zero live importers | accepted | Dashboard architecture / G8-P4 | open — not deleted; frozen-test protected (`s001EngineeringMode.test.ts`, `step065Ownership.test.ts`, `s004SettingsCutover.test.ts`, `s006EngineeringIsolation.test.ts`). `components/settings/{LlmRuntimePanel,OllamaModelsPanel}.tsx` in the same folder are **not** debt — still live/shared |
| DEBT-S008-03 | S008 | `components/overview/{AnalyticsPreviewRow,AnalyticsPreviewSection,KnowledgeBaseStatusCard,OverviewFooterNote,OverviewUrlCard,StatusSummaryCard,UrlInfoCard,RequestsLineChartCard,SystemStatsCard,OverviewGrid}.tsx` fully orphaned since S007 widget redistribution; zero live importers | accepted | Dashboard architecture / G8-P4 | open — not deleted; deletion not required by any acceptance criterion |
| DEBT-S008-04 | S008 | `components/overview/OverviewKnowledgeOsPanel.tsx` orphaned in production but frozen-test-imported (`step065Ownership.test.ts` reads it via `?raw`) | accepted | Dashboard architecture / G8-P4 | open — cannot be deleted without breaking accepted S001-era evidence |
| DEBT-S008-05 | S008 | `G9-P2`/`G9-P3` protocols authored but not executed — no cold-demo trial or accessibility scan has been run against a live instance | accepted | Product Readiness | open — execution is separate, later, explicitly authorized work |
| DEBT-G10 | S001–S008 (carried every Step) | `deploy/lib/verify_release.sh` `/tmp` ownership false-negative fix (`G10-P1`) and operator messaging clarity (`G10-P2`) not implemented | excluded (optional, independent of Dashboard program) | Release engineering tooling | open — explicitly out of scope for every Dashboard Step per operator instruction; deploy-time verify remains authoritative |

## Notes on resolved vs superseded rows

Rows marked **resolved** were fully closed by a later Step and require no further action. Rows marked **superseded** were the correct description of debt at the time they were written, but a later Step's evidence (S007/S008) describes the same underlying files more precisely/completely — the superseding row is the current source of truth; the original row is kept for audit trail, not duplicated as open debt twice.
