# Release 0.8 — Step 057: Engineering Closure

**RFC-100 Step 057**  
**Date:** 2026-07-28 (docs aligned 2026-07-29 for schema-first `migrate release`)  
**Lifecycle:** Release 0.8 **Engineering Ready** — Staging Validated **false** — Production Ready **false**

---

## Purpose

Close the engineering release for RFC-100 Steps **052–056**. This checkpoint records acceptance, rollback, build metadata, and validation. It does **not** deploy, apply migrations **0018/0019** to `ai_site_agent`, enable Memory assist/shadow, mutate Qdrant/corpus, or start Release **0.9**.

---

## Deliverables

| Artifact | Path |
|----------|------|
| Acceptance report | [RELEASE-0.8-ACCEPTANCE-REPORT.md](RELEASE-0.8-ACCEPTANCE-REPORT.md) |
| Rollback guide | [0.8-rollback.md](0.8-rollback.md) |
| This closure note | `0.8-step-057-release-closure.md` |
| Architecture review | [0.8-step-057-architecture-review.md](0.8-step-057-architecture-review.md) |
| Build metadata | `APP_RELEASE = "0.8"`, `closed_0_8: true` |
| Pre-deploy plan | [RELEASE-0.8-PRE-DEPLOY-PLAN.md](../operations/RELEASE-0.8-PRE-DEPLOY-PLAN.md) |
| Machine migration plan | [POST-0.8-MACHINE-MIGRATION.md](../operations/POST-0.8-MACHINE-MIGRATION.md) |
| Architecture snapshot | [KNOWLEDGE_OS_ARCHITECTURE_SNAPSHOT_0.8.md](../architecture/KNOWLEDGE_OS_ARCHITECTURE_SNAPSHOT_0.8.md) |

---

## Build metadata contract

| Field | Value |
|-------|-------|
| `APP_RELEASE` | `"0.8"` |
| `accepted` | `"0.8"` |
| `closed_0_6` | `true` |
| `closed_0_7` | `true` |
| `closed_0_8` | `true` |
| `engineering_ready` | `true` |
| `staging_validated` | **`false`** |
| `production_ready` | **`false`** |
| `in_progress` | `null` |

Capability metadata includes Steps **052–056** under `steps_052_057` / `release_0_8_capabilities`.

**Deploy lag:** a live `/api/build` process reports the new release status only after the closed code is **deployed and the backend restarted**. Until then, a running older process may still show prior `release_status`.

**Migrations:** code Alembic head is **0019**. This closure does **not** imply 0018/0019 are applied live.

---

## Classification

| State | Value |
|-------|-------|
| Engineering Ready | **PASS** |
| Staging Validated | **false** |
| Production Ready | **false** |
| Deployment | **not executed** |
| Migrations 0018/0019 live | **not applied** |
| Machine migration | **planned, not executed** |

---

## Known risk carried into ops

Step **055** default-off doc-type canonical path may leave **news → about → homepage** ordering on overview queries. Documented in acceptance + rollback. Not a blocker for Engineering Ready.

---

## HARD SAFETY — do not deploy until migrate release proves 0019

For this Release **0.8** cutover, **do not** run `deploy full` until `migrate release` has:

- exited successfully;
- reported the target **origin/main** commit;
- reported repository head **`0019_legacy_doc_type_canonical_enabled`**;
- verified live DB post revision is **0019**;
- verified both new Settings columns exist.

If these checks do **not** pass: **STOP. Do not deploy.**

Bare `migrate` / `migrate live` use the **currently deployed `/opt` migration tree** and **cannot** advance past migrations present in `/opt`. They are **not** a substitute for `migrate release` while `/opt` is still Release 0.7.

---

## Post-closure operations plan (document only — do not execute now)

**Operator entry point:** `bash deploy/manage_deploy.sh <command>` (`help` for syntax).

Canonical Release 0.8 sequence (identical everywhere):

```text
status
→ backup db
→ migrate release
→ verify schema head
→ deploy full
→ health
→ build-info
→ smoke
→ verify-release
```

```bash
bash deploy/manage_deploy.sh status
bash deploy/manage_deploy.sh backup db
bash deploy/manage_deploy.sh migrate release
# STOP unless report shows origin/main tip, head 0019, post revision 0019, columns OK
sudo bash deploy/manage_deploy.sh deploy full
bash deploy/manage_deploy.sh health
bash deploy/manage_deploy.sh build-info
bash deploy/manage_deploy.sh smoke
bash deploy/manage_deploy.sh verify-release
```

Then: manual dashboard / Sources / Chat / Understanding; preset **410** checks; Step 055 overview/news quality; corpus / Qdrant before–after. Only then consider Staging Validated under separate approval. Next program: [POST-0.8-MACHINE-MIGRATION.md](../operations/POST-0.8-MACHINE-MIGRATION.md) — **before** Release 0.9.

See [RELEASE-0.8-PRE-DEPLOY-PLAN.md](../operations/RELEASE-0.8-PRE-DEPLOY-PLAN.md) for the migration execution graph and policy-vs-enforcement note.

---

## Explicit confirmations

- Release 0.8 engineering closure **completed** in repository.
- Step **057** engineering closure **completed**.
- Release **0.9** **not started** and **blocked** until post-0.8 machine migration is accepted (project governance).
- **No** deployment performed.
- **No** migration applied to `ai_site_agent`.
- **No** live Settings mutation.
- Memory Assist/Shadow **not** enabled.
- Qdrant **unchanged** (not accessed for mutation).
- `ai_site_agent_recovery` **untouched**.
- Machine migration **not** executed.
