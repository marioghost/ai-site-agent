# RFC-100 Step 054 — Implementation note (Option B)

**Status:** implemented on `feat/step-054-disable-legacy-kp-presets` (pending review)  
**Architecture:** `docs/releases/0.8-step-054-architecture-review.md`  
**Baseline:** `570faccf0e67b62f8ccd1f04a3e5d4d7a00b62b4`

## Delivered

- Settings column `allow_legacy_kp_presets` (default **false**) via Alembic **`0018_allow_legacy_kp_presets`**
- ORM + Settings GET/PUT exposure
- Canonical helper `feature_flags.allow_legacy_kp_presets(settings)`
- `GET /api/knowledge-profile/presets` and `POST .../presets/load` → **HTTP 410** when false  
  Detail: `{ "code": "legacy_kp_presets_disabled", "message": "..." }`
- When true: prior 200 behavior + Deprecation headers on load preserved
- Dashboard: 410-aware KP page (hide preset selector, EN/UK info banner); import/edit/wizard remain
- Migration flags panel lists the flag (read-only); rollback via Settings API

## Explicitly unchanged

- In-process `PRESETS` / industry builders
- Generation / `structure_analyzer` / `profile_assembler`
- Stored `knowledge_profile_json` (no rewrite)
- Ranking / DocumentScorer / RPS / cache namespaces
- Reasoning / Evidence Assembly / Memory / Language / Qdrant

## Rollback

`PUT /api/settings` with `allow_legacy_kp_presets: true`.

## Deploy note

Do **not** apply migration 0018 to `ai_site_agent` until an approved deploy. Release 0.8 is **not** complete.
