"""Modular retrieval engine — document-first RAG pipeline.

Import leaf modules directly (e.g. ``retrieval_engine.pipeline``). Eager
re-exports here previously participated in an import cycle during
maintenance migrate.
"""
