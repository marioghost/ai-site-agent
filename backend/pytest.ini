[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    unit: pure unit tests that do not require POSTGRES_TEST_URL
    integration: tests that require POSTGRES_TEST_URL and optional env flags
    benchmark: optional wall-clock / performance checks — excluded from release-check
